<div class='container'>
  <div class='row my-3'>
    <h3>
      <%= link_to show_users_path(@user) do %>
        <%= @user.handle_name %>
      <% end %>
      のしおり
    </h3>
  </div>
  <div class='row'>
    <div class='col-md-8 mb-5'>
      <div class ="row">
        
      <div class='row'>
        <% if @book.user == current_user %>
          <%= link_to itinerary_path(@itinerary), method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-caution" do%>
          削除
          <% end %>
          <%= link_to itinerary_path(@itinerary), method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-caution" do%>
          編集
          <% end %>
        <% end %>

  <div class='row'>
    <div class='col-md-8 mb-5'>
      <%= render 'public/users/user_info', user:@user %>
    </div>
    <div class='col-md-7 offset-md-1 '>
    </div>
  </div>
</div>
